{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nuxtjs",
  "installCommand": "rm -rf node_modules && npm install --no-workspaces --include=optional",
  "buildCommand": "npx nuxt build"
}
